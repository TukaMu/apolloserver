export enum UserType {
    teacher = "teacher",
    customer = "customer",
}

export enum AllUserType {
    teacher = UserType.teacher,
    customer = UserType.customer,
    root = "root",
    open = "open"
}